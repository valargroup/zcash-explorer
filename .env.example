# =============================================================================
# Zcash Explorer Configuration
# =============================================================================
# Copy to .env and fill in the secrets

# -----------------------------------------------------------------------------
# Shared Configuration
# -----------------------------------------------------------------------------

# Zebra JSON-RPC (host.docker.internal reaches host from container)
ZCASHD_HOSTNAME=host.docker.internal
ZCASHD_USERNAME=zcashrpc
ZCASHD_PASSWORD=changeme

# Lightwalletd gRPC
LIGHTWALLETD_ENABLED=true
LIGHTWALLETD_HOSTNAME=host.docker.internal
LIGHTWALLETD_TLS=false

# Phoenix
EXPLORER_SCHEME=https

# Optional Kresko metadata discovery for the public RPC page
# KRESKO_RPC_NODES_DIR=/root/kresko/nodes
# KRESKO_RPC_PAYLOAD_DIR=/root/kresko/payload
# KRESKO_RPC_CONFIG_PATH=/root/kresko/config.json

# Optional testnet faucet. Kresko should import the matching private key into
# zcashd; the explorer only needs the public transparent source address.
FAUCET_ENABLED=false
# FAUCET_SOURCE_ADDRESS=tm...
FAUCET_AMOUNT=0.1
FAUCET_DAILY_IP_LIMIT=10
FAUCET_WINDOW_SECONDS=86400
FAUCET_MIN_CONFIRMATIONS=1
FAUCET_OPERATION_POLL_ATTEMPTS=30
FAUCET_OPERATION_POLL_INTERVAL_MS=1000

# Viewing Key feature
VK_CPUS=0.3
VK_MEM=1024M
VK_RUNNER_IMAGE=nighthawkapps/vkrunner

# -----------------------------------------------------------------------------
# Mainnet Configuration
# -----------------------------------------------------------------------------
# Generate with: mix phx.gen.secret
MAINNET_SECRET_KEY_BASE=<generate-secret-here>
MAINNET_EXPLORER_HOSTNAME=explorer.zec.pro

# -----------------------------------------------------------------------------
# Testnet Configuration
# -----------------------------------------------------------------------------
# Generate with: mix phx.gen.secret
TESTNET_SECRET_KEY_BASE=<generate-secret-here>
TESTNET_EXPLORER_HOSTNAME=explorer.testnet.zec.pro
